uniform float time;

void main( void ) {
	vec2 center = vec2(400,300);
	vec2 rad = gl_FragCoord.xy - center;
	float r = length(rad);
	theta = atan(rad.y, rad.x);
	
	if(r - theta < 0.00001) {
		gl_FragColor = vec4(sin(theta),cos(theta),0,1);
	} else {
		gl_FragColor = vec4(0,0,0,1);
	}
}